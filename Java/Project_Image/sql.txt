 CREATE TABLE STUDENT_INFO
   (	ROLL NUMBER(*,0) NOT NULL ENABLE, 
	FIRST_NAME VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	LAST_NAME VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	CLASS NUMBER(*,0) NOT NULL ENABLE, 
	YEAR NUMBER(*,0) NOT NULL ENABLE, 
	BIRTH_DATE DATE NOT NULL ENABLE, 
	MOBILE_NO NUMBER(*,0) NOT NULL ENABLE, 
	SEX VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	PERMANENT VARCHAR2(50 BYTE) NOT NULL ENABLE, 
	PRESENT VARCHAR2(50 BYTE) NOT NULL ENABLE, 
	TEACHERS_ID NUMBER(*,0) CONSTRAINT STUDENT_INFO_TEACHER_INFO_FK REFERENCES TEACHER_INFO (TEACHERS_ID), 
	 CONSTRAINT "STUDENT_INFO_PK" PRIMARY KEY (ROLL));